export default class itemModel {
    constructor(itemId, itemName, qty, price) {
        this.itemId = itemId;
        this.itemName = itemName;
        this.qty = qty;
        this.price = price;
    }
}